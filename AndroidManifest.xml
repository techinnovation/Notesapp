<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.squirrel.notesapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="17" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
         <!-- Slide Show activity -->
        <activity
            android:name="com.squirrel.notesapp.SlideShowActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Landing Page activity -->
        <activity
            android:name="com.squirrel.notesapp.LandingActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
        </activity>
        
        <!--  LoginActivity -->
        <activity
            android:name="com.squirrel.notesapp.LoginActivity"
            android:label="@string/app_name" 
            android:logo="@drawable/ic_launcher">
            
        </activity>
        
        <!-- SignupPage activity -->
        <activity
            android:name="com.squirrel.notesapp.SignupActivity"
            android:label="@string/app_name"            
            android:logo="@drawable/ic_launcher">
        </activity>
        <!-- main Activity -->
        <activity
            android:name="com.squirrel.notesapp.NotesActivity"
            android:label="@string/app_name"
            android:logo="@drawable/ic_launcher">
        </activity>
        
        <activity
            android:name="com.squirrel.notesapp.CoursePage"
            android:label="@string/app_name"
            android:logo="@drawable/ic_launcher">
        </activity>
        
         <activity
            android:name="com.squirrel.notesapp.NotesContents"
            android:label="@string/app_name"
            android:logo="@drawable/ic_launcher">
        </activity>
        
        <activity
            android:name="com.squirrel.notesapp.CoursenotesActivity"
            android:label="@string/app_name"
            android:logo="@drawable/ic_launcher">
        </activity>
        
        <activity
            android:name="com.squirrel.notesapp.SettingsActivity"
            android:label="@string/app_name"
            android:logo="@drawable/ic_launcher">
        </activity>
    </application>

</manifest>
